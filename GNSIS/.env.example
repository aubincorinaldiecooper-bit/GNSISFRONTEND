# URL of the gnsisbackend FastAPI service (no trailing slash), e.g.
# https://gnsisbackend-production.up.railway.app
VITE_API_BASE_URL=

# Only needed if the backend sets GNSIS_API_KEY. Sent as `Authorization: Bearer <key>`.
# VITE_API_KEY=
